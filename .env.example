# Frontend calls the API via same-origin /api (Next.js rewrites to backend).
# Default: /api (proxy). Set API_BACKEND_URL on the server for production.

# Client-side: base URL for API requests (default /api = proxy, no CORS)
NEXT_PUBLIC_API_URL=/api

# Server-side only (next build/start): backend target for /api rewrite
# Local dev: not set → rewrites to http://localhost:8080
# Production: set to https://api.gtd.co.id
# API_BACKEND_URL=https://api.gtd.co.id

# Client auth (for liveness etc.)
NEXT_PUBLIC_CLIENT_ID=your_client_id_here
NEXT_PUBLIC_CLIENT_SECRET=your_client_secret_here
